const pessoa = {
    nome: "Rebeca",
    idade: 2,
    peso: 13
}

// Pega as chaves
console.log(Object.keys(pessoa))

// pegar os valores
console.log(Object.values(pessoa))

// pegar chave e valor
console.log(Object.entries(pessoa))